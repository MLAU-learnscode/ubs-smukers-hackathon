PORT=3000
NODE_ENV=development

# Base URL for the "school days" map service (GET {MAP_SERVICE_BASE_URL}/graph?map_id=...)
# used by the "go" MCP tool. Confirmed live via curl; override only if it changes.
MAP_SERVICE_BASE_URL=https://tool-box-2591eaa24fa3.herokuapp.com

# Fixed study-materials index (GET returns {"documents":[{id,title,url},...]})
# used by the "search" MCP tool. Confirmed live via curl; override only if it changes.
STUDY_MATERIALS_INDEX_URL=https://tool-box-2591eaa24fa3.herokuapp.com/study-materials
